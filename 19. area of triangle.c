#include <stdio.h>
int main() {
    int base,height,area;
    printf("enter the base:");
    scanf("%d",&base);
    printf("enter the height:");
    scanf("%d",&height);
    area=0.5*base*height;
    printf("%d",area);
    return 0;
}
